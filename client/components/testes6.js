var drawES2015Chart = ({bcords, radius}) => (
  console.log("testing")
);


// function drawES2015Chart = ({bcords, radius}) => (
//   console.log( bcords, radius)
//   // do some chart drawing
// )

console.log(drawES2015Chart({
  bcords: {x: 18, y: 30},
  radius: 30
}));